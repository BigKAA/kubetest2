pipeline {
    agent {
        docker { image 'node:14-alpine' }
    }
    stages {
        stage ('test') {
            steps {
                sh 'echo "Helo world"'
                sh 'echo "biuld id: ${BUILD_ID}"'
                sh 'node --version'
            }
        }
    }
}